#ifndef MEPUTIL_H
#define MEPUTIL_H

/* MEPhisto's MISCellaneous
 * just a collection of possibly
 * useful functions */
#include <stddef.h>

// fill an array with random ints
void randiarr(int*, size_t, int, int);

// fill an array with random doubles
void randdarr(double*, size_t, double, double);

#endif
